# 备份和删除功能说明

## 📋 功能介绍

转换完成后，工具会自动执行以下操作：
1. ✅ **创建原始图片备份** - 保存原始图片文件夹的完整副本
2. ✅ **删除原始图片** - 删除转换后的图片文件夹中的原始图片

## 🚀 使用方式

### 方式 1：GUI（推荐）
```
启动图片转换工具_V2.bat

1. 选择图片文件夹
2. 设置目标尺寸
3. 点击"🚀 开始转换"
4. 自动完成备份和删除
```

转换完成后，会自动：
- 📦 在**同级目录**创建 `原文件夹名_backup_[时间戳]` 文件夹（包含所有原始图片）
- 🗑️ 删除原文件夹中的所有原始图片（只保留 .bin 文件）

### 方式 2：命令行（默认启用备份和删除）
```bash
python image_converter_tool.py -i "C:\Users\yinbolive\Desktop\test1" -w 416 -hh 240
```

### 方式 3：命令行（可选参数）
```bash
# 启用备份，启用删除（默认）
python image_converter_tool.py -i images/ -w 416 -hh 240 --backup --delete

# 禁用备份，启用删除
python image_converter_tool.py -i images/ -w 416 -hh 240 --no-backup --delete

# 启用备份，禁用删除
python image_converter_tool.py -i images/ -w 416 -hh 240 --backup --no-delete

# 禁用备份和删除
python image_converter_tool.py -i images/ -w 416 -hh 240 --no-backup --no-delete
```

## 📊 命令行参数详解

| 参数 | 说明 | 默认值 |
|------|------|-------|
| `-i, --input` | 输入图片文件或目录（必需） | - |
| `-o, --output` | 输出 .bin 文件目录（可选） | 同输入目录 |
| `-w, --width` | 目标宽度（像素） | 416 |
| `-hh, --height` | 目标高度（像素） | 240 |
| `-b, --backup` | 创建备份（启用） | ✅ 启用 |
| `--no-backup` | 禁用备份 | - |
| `-d, --delete` | 删除原始图片（启用） | ✅ 启用 |
| `--no-delete` | 禁用删除原始图片 | - |
| `-v, --verbose` | 显示详细信息 | - |

## 💾 备份文件位置

备份文件夹会自动创建在**原图片文件夹的同级目录**：

```
Desktop/
├── test1/                              # 原始图片文件夹
│   ├── test.bin                        # 转换后的二进制文件
│   ├── test1.bin
│   └── test2.bin
└── test1_backup_20251222_144807/      # 自动创建的备份
    ├── test.jpg                        # 原始图片备份
    ├── test1.jpg
    └── test2.jpg
```

## 🔄 完整流程示例

### 转换前
```
C:\Users\yinbolive\Desktop\test1\
├── test.jpg           (800x800)
├── test1.jpg          (800x1422)
└── test2.jpg          (1080x1925)
```

### 执行转换
```bash
python image_converter_tool.py -i "C:\Users\yinbolive\Desktop\test1" -w 416 -hh 240
```

### 转换后
```
C:\Users\yinbolive\Desktop\
├── test1\
│   ├── test.bin       ✨ 新生成
│   ├── test1.bin      ✨ 新生成
│   └── test2.bin      ✨ 新生成
│   （原始图片已删除）
│
└── test1_backup_20251222_144807\    ✅ 自动备份
    ├── test.jpg
    ├── test1.jpg
    └── test2.jpg
```

## ⚠️ 重要提示

### 🔐 安全性
- ✅ **自动备份** - 原始图片不会丢失，备份文件夹保存在安全位置
- ✅ **时间戳** - 每次备份都带有时间戳，不会相互覆盖
- ✅ **完整保存** - 备份包含 100% 的原始数据

### ⏹️ 如何禁用删除
如果你不想删除原始图片，使用：
```bash
python image_converter_tool.py -i images/ -w 416 -hh 240 --no-delete
```

### 🔄 恢复原始图片
如果需要恢复原始图片：
1. 打开备份文件夹 `test1_backup_[时间戳]/`
2. 复制所有文件到原文件夹 `test1/`

## 📝 日志输出

转换完成时会显示：

```
============================================================
🔄 转换完成，执行后处理...
============================================================

💾 创建备份...
   源目录: C:\Users\yinbolive\Desktop\test1_temp
   备份路径: C:\Users\yinbolive\Desktop\test1_temp_backup_20251222_144807
   ✅ 备份创建成功!

🗑️ 删除原始图片...
   目录: C:\Users\yinbolive\Desktop\test1_temp
   ✅ 已删除 3 个图片文件
```

## 🧪 测试结果

```
✅ .bin 文件生成正确
✅ 原始图片已全部删除
✅ 备份文件夹已创建

🎉 所有测试通过！功能正常工作！
```

---

## 快速参考

| 需求 | 命令 |
|------|------|
| 转换并备份+删除（推荐） | `python image_converter_tool.py -i images/ -w 416 -hh 240` |
| 仅转换（保留原始图片） | `python image_converter_tool.py -i images/ -w 416 -hh 240 --no-delete --no-backup` |
| 转换+删除但不备份 | `python image_converter_tool.py -i images/ -w 416 -hh 240 --no-backup --delete` |
| 转换+备份但不删除 | `python image_converter_tool.py -i images/ -w 416 -hh 240 --backup --no-delete` |

---

**推荐使用默认设置（启用备份和删除）**，这样既能清理空间，又能保证数据安全！✅
